#include <stdio.h>
#include <stdlib.h>

int main() {
    int a = 5;
    int b = 10;

    // Logica relacional
    if (a > b) {
        printf("a e maior que b.\n");
    } else if (a < b) {
        printf("a e menor que b.\n");
    } else {
        printf("a e igual a b.\n");
    }

    return 0;
}