#include <stdio.h>
#include <stdlib.h>

int main() {
    // Desenvolver um programa que imprima o produto entre 28 e 43
    // Declarando as variáveis
    int num1 = 28;
    int num2 = 43;

    // Calculando o produto
    int produto = num1 * num2;

    // Imprimindo o resultado
    printf("O produto entre %d e %d e: %d\n", num1, num2, produto);

    system("pause");
    system("cls");

    return 0;
}